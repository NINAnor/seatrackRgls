# Seatrack filter settings

Default seatrack filter settings in as `GLSFilterSettingsList` object.

## Usage

``` r
seatrack_settings_list
```

## Format

An object of class `GLSFilterSettingsList` (inherits from `R6`) of
length 5.
