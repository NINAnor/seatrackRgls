% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_a_folder_wrappers.R
\name{prepare_calibration}
\alias{prepare_calibration}
\title{Prepare Calibration Data from a Folder}
\usage{
prepare_calibration(
  import_directory,
  metadata,
  all_colony_info,
  output_directory,
  show_filter_plots = FALSE,
  export_calibration_template = TRUE,
  filter_setting_list = seatrackRgls::seatrack_settings_list
)
}
\arguments{
\item{import_directory}{Directory containing raw light data files. These are expected to be named in the format \verb{<logger_id>_<year_retrieved>_<logger_model>}, e.g. \code{C23_2015_mk4083}}

\item{metadata}{A data frame containing calibration data for all loggers or a string providing a filepath to read this data from. This can be from an excel file, CSV file or a directory containing multiple calibration files.
This dataframe can consist of a single row per logger/year combination, with the following columns \code{logger_id}, \code{species}, \code{colony}, \code{date_deployed} and \code{date_retrieved}. Providing \code{logger_model} is strongly advised.
These will be automatically split into time windows, using the \code{year_split} setting (defaulting to "01-06" in all 'seatrack_settings_list').
Alternatively, custom time windows can be provided by including columns \code{start_datetime} and \code{end_datetime}, with one time window per row.}

\item{all_colony_info}{A data frame containing colony information for all loggers (one row per colony). The required columns are \code{colony}, \code{latitude}, and \code{longitude}.}

\item{output_directory}{Directory to save calibration templates and calibration plots. If \code{show_filter_plots} is \code{TRUE}}

\item{show_filter_plots}{A logical indicating whether to show individual filter plots for the default sun angle. Defaults to \code{FALSE}.}

\item{export_calibration_template}{When \code{TRUE} will export a calibration template in excel format. Otherwise returns the calibration template as an R object. Defaults to \code{TRUE}.}

\item{filter_setting_list}{A 'GLSFilterSettingsList' object containing filter settings for loggers or a path to load one using \code{\link[=read_filter_file]{read_filter_file()}}. Defaults to 'seatrackRgls::seatrack_settings_list'.}
}
\description{
Convience function for preparing GLS logger data for light data calibration. Uses \code{process_folder()} in calibration mode to handle a directory of light data. See 'process_folder()' for more details.
Custom settings can be provided using \code{filter_setting_list}. For the creation of an empty filter settings template see \code{create_filter_settings_file()}.
If \code{export_calibration_template} is \code{TRUE}, it will export an empty calibration sheet using 'calibration_to_wb()'.
}
\details{
Will export calibration plots to assist in choosing appropriate sun angles for processing GLS logger data (see vignette). Once these are decided on, final positions can be exported using process_positions()
}
\concept{processing_wrapper}
